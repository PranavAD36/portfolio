# Custom Cursor Notes

The portfolio uses a desktop-only custom cursor with a subtle tail-like motion and a refined glassy core. The default browser cursor is hidden globally so only the custom effect remains visible.

## Behavior

- Hidden on touch and coarse-pointer devices
- Responsive to hover states on interactive elements
- Preserved as a lightweight effect to avoid harming performance
