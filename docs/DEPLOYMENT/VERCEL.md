# Vercel Deployment

## Required Environment Variable

Set the following variable in Vercel:

- NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY

## Notes

The contact form expects the access key to be available at runtime in the browser environment, so the variable must be exposed to the Next.js app and configured in Vercel.
